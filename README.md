Joyce Byers (of Stranger Things) made a Ouija board on her wall with christmas lights. This implements the lights.

Installation requirements:
libffi-dev
libss-dev

Then run `pip install -r requirements.txt`.
